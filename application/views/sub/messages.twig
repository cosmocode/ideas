<div class="messages">
    <noscript>
        <div class="alert">
            <strong>Attention!</strong> You don't have JavaScript enabled, some parts of the system will not work properly!
        </div>
    </noscript>
    {% for msg in _messages %}
        <div class="alert alert-{{msg.type}}">
            <button class="close" data-dismiss="alert">×</button>
            {{msg.text|raw}}
        </div>
    {% endfor %}
</div>